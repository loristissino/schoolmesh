#!/bin/bash

if [ $# -eq 0 ]
	then
		echo "This script is a wrapper. See schoolmesh(1) for details."
		exit 1
	fi

VALUE=$(sudo quota --no-wrap -v -u $1 | tail -1)
echo ${VALUE:16:7}:${VALUE:24:7}:${VALUE:32:7}:${VALUE:40:7}:${VALUE:48:7}:${VALUE:56:7}:${VALUE:64:7}:${VALUE72:7}