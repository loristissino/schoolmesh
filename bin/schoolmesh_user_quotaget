#!/bin/bash

if [ $# -eq 0 ]
	then
		echo "This script is a wrapper. See schoolmesh(1) for details."
		exit 1
	fi

sudo quota --no-wrap -v -u $1 | tail -1| gawk '{print $2 ":" $3 ":" $4 ":" $5 ":" $6 ":" $7}'
